<!-- filled by /dsf:concept — start from this skeleton, do not restructure it -->

# Concept

The reasoning behind the visual language. `concept/concept.html` is the live test stand; this
file is why it looks like that. A palette guessable from the category is a model reflex, not a
decision — it is thrown out before it is ever shown.

---

## Designer's taste

> **HUMAN GATE.** Asked for **before anything is written**. Taste is data, and it lives in the
> file, not in the human's head. Do not invent it, do not infer it from the research, do not
> proceed without it.

**Likes — 2 to 3 named products**, not adjectives, each with what exactly is liked about it:

| Product | What exactly is liked | In the human's own words |
|---|---|---|
| `[?]` | `[?]` | `[?]` |

**Anti-references** — what is definitely not wanted, **including the obvious first reflex for
this category**:

| Anti-reference | Why not | In the human's own words |
|---|---|---|
| `[?]` | `[?]` | `[?]` |

---

## Attributes — 3 to 5 pairs of opposites

Each pair states which line of data it is built from **and** which borrowed device from
`concept/references.md` carries it. A pair without an explanation is an invention.

**No pair may contradict "Designer's taste".** If the data and the taste pull in opposite
directions, say so plainly here and let the human decide — do not average them.

<!-- a pair is two opposites that a screen could genuinely be either of ("this, not that"); a
     single adjective with "very" in front of it is not a pair, and neither is a pair whose
     second half nobody would ever choose -->
<!-- the data line is quotable: a named line of personas.md, jtbd.md or research.md, not "the
     research suggests" -->
<!-- the borrowed device names a row of concept/references.md, so every attribute can be traced
     to something that already exists in the world -->


| # | Pair (this, not that) | Data line it is built from | Borrowed device that carries it |
|---|---|---|---|
| 1 | `[?]`, not `[?]` | `[?]` | `[?]` |
| 2 | `[?]`, not `[?]` | `[?]` | `[?]` |
| 3 | `[?]`, not `[?]` | `[?]` | `[?]` |

**Conflicts between data and taste, and how the human resolved them:**

| Conflict | Data says | Taste says | Human's decision |
|---|---|---|---|
| `[?]` | `[?]` | `[?]` | `[?]` |

---

## Directions — three contrasting

Genuinely different decisions, not three shades of one. Shown live and side by side in
`concept/directions.html`: palette with hex values, a type pair, a real card of the product's
primary object with a real photo by content theme, a button, a badge, an icon set in one style.

### Direction A — `[?]`

- **Palette:** `[?]` (hex values live in `directions.html`)
- **Type pair:** `[?]`
- **Form:** `[?]`
- **Photo treatment:** `[?]`
- **Icons:** `[?]`
- **Which attributes it leans on:** `[?]`

### Direction B — `[?]`

- **Palette / type / form / photo / icons:** `[?]`
- **Which attributes it leans on:** `[?]`

### Direction C — `[?]`

- **Palette / type / form / photo / icons:** `[?]`
- **Which attributes it leans on:** `[?]`

**Reflex directions rejected before the page was shown:** `[?]` — see
`concept/references.md` § Rejected.

---

## Chosen direction

> **HUMAN GATE.** The human opens `concept/directions.html` in a browser and names the chosen
> direction. The agent never picks.

- **Picked:** `[?]` (number and name)
- **The human's reasoning, verbatim:** `[?]`
- **Base + borrowed:** base is `[?]`, borrowed `[?]` from `[?]` — never a clone.
- **Proven on:** `[?]` and `[?]` — exactly two contrasting screens, with all their state pages.

### The two rejected directions, kept

<!-- kept because a later phase may want to return to them -->

| Direction | What it did well | Why it lost |
|---|---|---|
| `[?]` | `[?]` | `[?]` |

---

## Decisions and their attributes

Every visual decision on the stand traces to an attribute or to recorded taste. A decision that
traces to neither is not a decision — either name the source or drop it.

| Decision (colour · type · form · icon · photo) | Value | Attribute or taste line it serves |
|---|---|---|
| `[?]` | `[?]` | `[?]` |

### Point edits after "keep it"

<!-- an edit lives where the language lives, not where it was noticed. Log what moved and why;
     if an edit contradicts an existing attribute, the attribute itself is changed here. -->

| What changed | On which screen it was noticed | Where it was fixed | Attribute updated? |
|---|---|---|---|
| `[?]` | `[?]` | `[?]` | `[?]` |
