<!-- filled by /dsf:voice — start from this skeleton, do not restructure it -->

# Voice

The contract for every product string. `voice/microcopy.md` is the source of truth for what the
strings **are**; this file is the source of truth for **how they are written**.

Voice is **rules, not mood**. An adjective ("friendly, expert, simple") cannot write a button.
Every principle carries a rule, an example, a counter-example and the line of data it comes
from. Three honest principles beat five pretty ones.

---

## Principles — 3 to 5

**At least one principle is derived from competitors' actual language.** Where everyone writes
the same way, that sameness is where voice creates difference. If `research/research.md` has no
language samples, collect 10–15 real strings from real competitor surfaces first, add them to
`research/research.md` under "Competitor language", and derive from those.

**No principle without a source line.**

### Principle 1 — `[?]`

<!-- rule — one sentence about how the product speaks, not an adjective; it must be possible to
     apply it to a single string and get a yes or a no -->
<!-- example and counter-example are real interface strings as they would appear on a screen,
     not descriptions of strings -->
<!-- source — a specific line, quote or sample, not "the research" -->

- **Rule** (one sentence, applicable to a concrete string): `[?]`
- **Example** (a real interface string written by the rule): `[?]`
- **Counter-example** (a string that breaks it): `[?]`
- **Source** (the line in `personas.md`, `research.md`, or the competitor-language section):
  `[?]`

### Principle 2 — `[?]`

- **Rule:** `[?]`
- **Example:** `[?]`
- **Counter-example:** `[?]`
- **Source:** `[?]`

### Principle 3 — `[?]`

- **Rule:** `[?]`
- **Example:** `[?]`
- **Counter-example:** `[?]`
- **Source:** `[?]`

---

## Dictionary — one concept, one word

For every drift marked in `voice/microcopy.md`, decide which term stays. Take terms from the
language of the personas and the research; invent none.

<!-- "why this one" — in the language of the personas and the research, not officialese; the
     "never use" column lists the actual rejected terms found in the inventory, not synonyms
     invented here -->

| Concept | The word we use | Never use | Why this one |
|---|---|---|---|
| `[?]` | `[?]` | `[?]` | `[?]` |

**Form of address:** `[?]` — used identically on every screen.

**Borrowed foreign words:** allowed — `[?]` · not allowed — `[?]`

---

## Banned — what we never write

Each entry with a **before/after** example. Without this list, the model default —
enthusiasm, exclamation marks, emoji — returns with every new prompt.

| Banned | Before | After |
|---|---|---|
| AI clichés ("Oops, something went wrong") | `[?]` | `[?]` |
| Motivational tone | `[?]` | `[?]` |
| Stray exclamation marks | `[?]` | `[?]` |
| Emoji in system messages | `[?]` | `[?]` |
| "successfully" | `[?]` | `[?]` |
| `[?]` | `[?]` | `[?]` |

---

## Microcopy rules

By element type and state tone. **Each rule carries one example from this product**, and each
one is checked against the principles and the dictionary above.

| Element / state | Rule | Example from this product |
|---|---|---|
| Button | An action verb whose result is visible — never "OK" or "Next" | `[?]` |
| Screen heading | What this place is, in dictionary words | `[?]` |
| Form field — label | Says what to enter | `[?]` |
| Form field — hint | Says how | `[?]` |
| Form field — validation error | Says exactly what to fix | `[?]` |
| Empty state | Why it is empty and what to do next | `[?]` |
| Error | What happened and what to do; no apologies, no jokes | `[?]` |
| Loading | Silent, or says exactly what is loading | `[?]` |
| Success | The fact and the next step; no celebration | `[?]` |
| Destructive action | Before the click: what will happen and what cannot be undone | `[?]` |
